services:
  wiremock:
    image: wiremock/wiremock:3.13.1
    container_name: termurl-wiremock
    ports:
      - "3000:8080"
    volumes:
      - ./mappings:/home/wiremock/mappings:ro
    command: ["--global-response-templating"]
