# termurl collection configuration
# Profiles map names to variable sets. Secrets always come from the
# environment, never from this file.

[profiles.dev]
host = "http://localhost:3000"

[profiles.staging]
host = "https://staging.example.com"

[profiles.prod]
host = "https://api.example.com"
