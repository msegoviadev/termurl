{
  "name": "termurl",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch index.ts",
    "tui": "bun run index.ts",
    "typecheck": "bunx tsc --noEmit --target ESNext --module NodeNext --moduleResolution NodeNext --skipLibCheck index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@opentui/core": "^0.4.5"
  }
}
