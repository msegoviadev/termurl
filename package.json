{
  "name": "termurl",
  "version": "0.3.0",
  "description": "A terminal client for Hurl collections, with an agent-friendly headless CLI",
  "module": "index.ts",
  "type": "module",
  "license": "MIT",
  "repository": "github:msegoviadev/termurl",
  "homepage": "https://github.com/msegoviadev/termurl",
  "private": false,
  "scripts": {
    "dev": "bun run --watch index.ts",
    "tui": "bun run index.ts",
    "build": "bun build --compile index.ts --outfile termurl",
    "typecheck": "bunx tsc --noEmit --target ESNext --module NodeNext --moduleResolution NodeNext --resolveJsonModule --skipLibCheck index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@opentui/core": "^0.4.5"
  }
}
